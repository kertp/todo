  <tr>
    <td><%= task.name %></td>
    <td><%= task.content %></td>
    <td><%= task.deadline %></td>
    <td><%= task.closed %></td>
    <td><%= link_to 'Show', project_task_path(:project_id => @project.id, :task_id => task.id)%></td>
  </tr>
<br />
